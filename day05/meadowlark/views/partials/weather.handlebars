<div class="weatherWidget">
    {{#each partials.weather.locations}}
        <div class="location">
            <h1>{{name}}</h1>
            <a href="{{forecastUrl}}">
                <img src="{{iconUrl}}" alt="{{weather}}"/>
                {{weather}},{{temp}}
            </a>
        </div>
    {{/each}}
</div>